import styled from "styled-components";

export const Main = styled.main`
	/* padding: 5rem; */
	height: 95vh;
	/* background-color: aqua; */
	border-color: 5px solid red;
	margin: 1.4rem 2rem;
	border-radius: 0.4rem;
	box-shadow: 1px -1px 12px 1px rgb(0, 0, 0, 0.7);
`;
